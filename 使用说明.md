# 划词翻译 · 使用说明

**版本** v1.1.5 ｜ **最后更新** 2026-09-15

---

# ⚠️ 使用前必读（关于隐私）

**你划中的文字会发送给「你自己配置的 AI 服务商」。**

请注意：

1. **开着本工具时，不要划选包含个人信息的文字**
   （身份证、银行卡、密码、私人对话、助记词、病历等）
2. **需要处理敏感内容时，先关掉本工具**
   按 `Alt+T`（或 `Alt+Shift+T`），屏幕底部会闪出「划词翻译：已关闭」
   **用完再按一次开回来**
3. 翻译内容只会发到你**自己配置的那个 AI**，不经过本工具作者，也不经过第三方翻译网站

> **为什么要有这条提醒**：翻译是把文字**发给云端 AI 去算**的，不是在你电脑上算的。
> 这是所有云端翻译工具的共同特点，不是本工具独有的问题。
>
> 另外说明：内容只发给**你自己配置的那个 AI**，
> 不经过本工具的作者，也不经过任何第三方翻译网站。

---

## 一、这是什么

在**任意网页**上拖动选中文字，松手就弹出气泡，显示**逐句中英对照**的翻译。

它和普通划词翻译插件的区别：

| 特点 | 说明 |
|---|---|
| **走你自己的 AI** | 默认借用你电脑上 DSH 配好的模型线路，不经过第三方翻译网站 |
| **代码不会被翻坏** | `npm ci`、`build.sh`、`v1.2.3`、commit SHA 原样保留，不被翻译 |
| **术语统一** | `repository` 一定是「仓库」，`fork` 一定是「分叉」 |
| **重复划不花钱** | 同一段文字第二次划，走缓存，零成本零延迟 |
| **只在能看懂时才翻** | 选中的内容没有字母（纯数字/验证码/卡号）不翻译 |

## 二、它由两块组成

```
① 浏览器里的脚本（油猴管理）
      负责：发现你划了什么、画气泡、显示译文
      ↑ 这个东西一直在，不需要"启动"

② 黑窗口（翻译代理）
      负责：拿着钥匙去问 AI，把译文拿回来
      ↑ 这个东西要开着
```

**两块缺一不可**：黑窗口关了 → 气泡还会弹，但里面写「翻译代理没在运行」。

---

## 三、一次性安装（4 步，⚠️ 顺序不能错）

### 第 1 步：装油猴

浏览器打开：
`https://microsoftedge.microsoft.com/addons/detail/iikmkjmpaadaobahmlepeloendndfphd`

点「获取」→「添加扩展」。

### 第 2 步：⚠️ 打开「允许用户脚本」开关（最容易漏的一步）

1. 地址栏输入 `edge://extensions` 回车
2. 找到 **Tampermonkey** → 点「**详细信息**」
3. 把「**允许用户脚本**」开关**打开**

> **漏了这一步，脚本装上了也永远不工作，而且不会有任何报错。**
> 这是新版 Edge/Chrome 的要求。

### 第 3 步：启动代理

双击 `启动翻译代理.cmd`，出现黑色窗口 → **最小化它，别关**。

### 第 4 步：装脚本

浏览器打开：`http://127.0.0.1:8787/install.user.js` → 点「安装」

> ⚠️ **必须先做第 3 步**——这个网址是代理提供的，代理不开就打不开。

**备用安装法**（代理起不来时）：用记事本打开 `easy-en-translate.user.js`
→ `Ctrl+A` 全选 → `Ctrl+C` → 油猴图标 →「添加新脚本」→ `Ctrl+A` 删掉模板
→ `Ctrl+V` 粘贴 → `Ctrl+S` 保存。

---

## 四、日常使用（三步）

```
① 双击 启动翻译代理.cmd      →  黑窗口出现，最小化（别关）
② 打开任意网页，拖动选中英文  →  松手，气泡出现
③ 不用了                     →  黑窗口留着也行；想关就双击 停止翻译代理.cmd
```

**重启电脑后要重新双击启动代理**（目前没有设开机自启）。

---

## 五、快捷键

| 按键 | 作用 |
|---|---|
| **`Alt+T`** | **开 / 关整个工具**（屏幕底部会闪出当前状态） |
| **`Alt+Shift+T`** | 同上，**备用键**（有些网站会占用 `Alt+T`） |
| `Esc` | 关掉当前气泡 |

> **开关是有记忆的**：关掉之后，刷新页面、重启浏览器都还是关的。

## 六、油猴菜单（点浏览器右上角油猴图标）

| 菜单项 | 作用 |
|---|---|
| 划词翻译：开启 / 关闭 | 同 `Alt+T` |
| 切换 中英对照 / 仅译文 | 气泡里要不要显示英文原文 |
| 切换引擎（本机代理 / 填的 key） | 临时换引擎（**只对本次浏览器会话有效**，想长期切换去设置页保存） |
| **检查本机翻译代理** | ⭐ **自检神器**：显示模型、线路、钥匙状态、缓存条数 |
| 清空翻译缓存 | 缓存文件变大或想重新翻译时用 |
| 查看引擎状态 | 看各个引擎有没有配好钥匙 |

---

## 七、⭐ 改了代码之后，怎么让它生效

**磁盘上的文件 ≠ 浏览器里正在跑的脚本。** 这是最容易搞错的地方。

| 你改了哪个文件 | 要做什么才生效 |
|---|---|
| **`easy-en-translate.user.js`**（脚本） | 打开 `http://127.0.0.1:8787/install.user.js` → 点「更新」→ **再刷新网页** |
| **`dsh-translate-proxy.js`**（代理） | **关掉黑窗口 → 重新双击启动** |
| `启动/停止翻译代理.cmd` | 不用管，下次双击就是新的 |

**口诀：改脚本 → 重装脚本；改代理 → 重启代理。两边都要刷新网页。**

> 为什么：油猴里存的是**它自己的一份副本**，改磁盘文件它不知道。

---

## 八、出问题怎么办

| 现象 | 最可能的原因 | 怎么办 |
|---|---|---|
| 划词**完全没反应** | ① 工具被关了 ② 页面没刷新 ③ 油猴的「允许用户脚本」没开 | 依次：按 `Alt+T`（看底部提示条）→ 按 `F5` → 查 `edge://extensions` |
| 气泡**弹了但写"翻译失败"** | 代理没在运行 | 看黑窗口在不在 → 气泡上点「重试」 |
| 提示**"翻译代理没在运行"** | 黑窗口被关了 | 双击 `启动翻译代理.cmd` |
| 想确认**一切正常** | — | 油猴菜单 →「检查本机翻译代理」 |
| 黑窗口**一闪而过** | `.cmd` 的行尾被改成 LF 了 | 别用记事本改 `.cmd`；要改就用 VS Code 并保持右下角显示 **CRLF** |
| 中文显示成乱码 | 控制台代码页 | 用 VS Code 的终端，或先执行 `chcp 65001` |
| **气泡关不掉** | 你可能点过「**钉住**」 | 看气泡右上角按钮：蓝底写「已钉住」就是钉住了，点一下它 / 按 `Esc` / 点 `✕` |
| 网易邮箱**正文**翻不了 | 正文在 iframe 里，脚本不进入 iframe（`@noframes`） | 这是已知限制，暂时无解 |

## 九、已知问题（都记着，将来处理）

| 问题 | 现状 |
|---|---|
| `Alt+T` 在**某些网站**失灵 | 用 `Alt+Shift+T` 或油猴菜单（已从 `document` 改到 `window` 提高成功率） |
| **整页可编辑的网站**（Notion、语雀、飞书文档） | 能正常用（我们试过"输入框内不翻译"，因为副作用太大已撤销） |
| **iframe 里的内容**翻不了 | 如网易邮箱正文、内嵌文档 |
| **Google Docs / 表格 / Office Online** | 文字画在 canvas 上，`getSelection()` 取不到，**无法支持** |
| 严格 CSP 的网站可能**气泡没样式** | 尚未遇到；真遇到就改用 `adoptedStyleSheets` |
| 和网站自带的划词弹窗**可能重叠** | 如知乎、CSDN。按 `Esc` 关我们的 |

---

## 十、文件说明

| 文件 | 作用 | 能删吗 |
|---|---|---|
| `easy-en-translate.user.js` | **划词脚本本体**（浏览器里跑的源码） | ❌ 不能（要用它重装） |
| `dsh-translate-proxy.js` | **代理本体**，黑窗口跑的就是它 | ❌ 不能 |
| `启动翻译代理.cmd` | 双击启动 | ❌ 不能 |
| `停止翻译代理.cmd` | 双击停止 | ⚠️ 可有可无（关窗口也行） |
| `.translate-cache.json` | **翻译缓存**（含你划过的原文+译文，**明文**）。第一次运行后才生成 | ✅ 能删（删了会重新花钱翻一遍） |
| `probe-llm.js` | 探查模型线路是否可用 | ✅ 测试工具 |
| `build-dict.js` | 把 ECDICT 的 CSV 转成本地词典（要重新生成词典时才用） | ✅ 测试工具 |
| `tests/` | 自动化测试（15 个脚本，见下面第九节） | ⚠️ 开发用，留着 |
| `README.md` | 给 GitHub 看的简介 | ⚠️ 留着 |
| `使用说明.md` | 就是这份详细文档 | ⚠️ 留着 |
| `LICENSE` | 开源许可证（MIT） | ⚠️ 留着 |

---

## 十一、未来计划

| 计划 | 说明 |
|---|---|
| **点单词查词典** | 点气泡里的原文单词 → 弹小卡片（音标、词义、变形、发音）；方案已定稿，未开工 |
| **公开给他人使用** | 去掉对代理的依赖（用户直接填自己的 key）+ 设置页；方案已定稿，未开工 |
| **修 CORS 漏洞** | 公开前必做（当前 `Access-Control-Allow-Origin: *` 让任何网站都能白嫖你的额度） |
| **气泡和站内弹窗错开位置** | 已推迟 |
| **开机自启代理** | 可不做 |

---

## 十二、v1.2 新增：点原文单词查词典

**用法**：划词弹出气泡后，**鼠标移到「原文」那一行里的某个英文单词上**（会微微高亮），**点一下**，旁边弹出词典卡片。

```
┌─ 词典卡片 ──────────────────────┐
│ token                🔊    ✕   │  ← 🔊 点一下朗读（浏览器自带，免费不联网）
│ /'tәukәn/                      │
│ [计] 记号                       │  ← 编程义：词典自带 [计]/[网] 标注，免费
│ n. 表征, 记号, 代币              │
│ a. 象征的, 表意的                │
│ [ 展开全部（还有 1 条中文释义）]   │
│ 变形：tokens                    │
│ 考试：六级 · 考研 · 雅思          │
│ ──────────────────────────      │
│ [ 💡 看这句话里的意思（AI）]      │  ← 点这个才会调 AI（花钱）
└────────────────────────────────┘
```

**几个设计说明**：

| 效果 | 说明 |
|---|---|
| **词典是本地查的** | 77 万词条存在本地，**2 毫秒出结果、不联网、不花钱** |
| **只有问 AI 才花钱** | 点「💡 看这句话里的意思」才调 AI。词典只知道"这个词有哪些意思"，**只有 AI 能判断"在这句话里是哪个意思"** |
| **代码不会被查** | `npm ci`、`index.js`、`v1.2.3` 这些整块跳过，不可点 |
| **拖选不会误触发** | 鼠标移动超过 5 像素就算拖选（复制），不会弹卡片 |
| **变形词自动还原** | 点 `committed` 会提示"原形是 commit" |
| `Esc` 分两级关闭 | 先关词典卡片，再按一次关气泡 |

**关掉这个功能**：油猴菜单 →「切换：点击原文单词查词典（开 / 关）」

---

## 十三、v2.0 新增：用自己的 AI（不用装代理）

### 为什么需要它

原来的架构必须跑本机代理（那个黑窗口）。别人要用就得：装 Node → 下载文件 → 双击 → **别关窗口**。这道门槛会筛掉大部分人。

**这一版让脚本能直接连 AI**：

```
原来：浏览器 → 本机代理（黑窗口）→ AI     要装 Node、要开窗口
现在：浏览器 ────────────────────→ AI     只填一个 key
```

### 怎么用（4 步）

```
① 点油猴图标 →「⚙ 设置自己的 AI（填 key，免装代理）」
② 在「AI 服务商」下拉里选一家（选了会自动填下面两格）
③ 把 API 密钥粘进去
④ 点「测试连接」→ 看到 ✅ 就点「保存」     ← 保存后自动切换到直连
```

### 支持哪些 AI

**只要接口是「OpenAI 兼容」格式就能用**（现在绝大多数都是）：

| 服务商 | 接口地址 | 模型示例 |
|---|---|---|
| **DeepSeek** | `https://api.deepseek.com` | `deepseek-chat` |
| OpenAI | `https://api.openai.com/v1` | `gpt-4o-mini` |
| OpenRouter（能调 Claude/Gemini） | `https://openrouter.ai/api/v1` | 看它列表 |
| 月之暗面 Kimi | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash` |
| 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` |

> **只填三个框就能用任何 AI**：接口地址 + 模型名字 + API 密钥。
> 只要接口是「OpenAI 兼容」格式就能用。设置页里选好服务商后会直接给出**去哪拿 key 的 4 步图文**。

### 代理还需要吗

| 你 | 要代理吗 |
|---|---|
| 只想划词翻译、看完事 | ❌ **不需要**，填个 key 就行 |
| 想要本地词典（点词查义） | ✅ **需要**——词典数据（86 MB）和查询服务在代理里 |
| 想要共享缓存、提示词可远程更新 | ⭕ 可选 |

**代理从"必需品"降级成"可选外挂"了。**

---

## 十四、v2.1.2：修了 5 个"不报错但一直默默在坑你"的 bug

这一版没加新功能，只修问题。修的这 5 个都属于**最难发现的那一类**：
程序不崩、不弹错，你只会觉得"翻译质量好像有点怪""有时候怎么划都没反应"。

| # | 症状（用户能感觉到的） | 真正的原因 |
|---|---|---|
| 1 | 译文时好时坏，遇到 `Node.js`、`v1.2.3`、`README.md`、`e.g.` 这种就明显变差 | 断句逻辑把句子里**词中间的点**当成了句号，一句话被切成 4 段碎片，每段单独发给 AI —— 上下文全丢了 |
| 2 | 报过一次"配置有误"之后，**怎么划都是同一句报错**，直到重启浏览器 | 那个"配置锁"记在脚本内存里，修好 key 也不会自动解开 |
| 3 | 报错提示让你去"检查脚本里的密钥"，可脚本里**根本没有密钥** | 那是只有本机代理时代才成立的说法，公开版忘了改 |
| 4 | 切引擎时提示"会自动退回 MyMemory" | 那个引擎 v2.0.6 就删掉了，纯属过时文案 |
| 5 | 关掉黑窗口后，**刚翻好的几条下次打开要重新翻**（要重新花钱） | 代理写缓存是延迟 1 秒的**异步**写，进程一退，那个定时器根本没机会跑 |

### 第 1 个最值得说

修之前：

```
原文：Use Node.js, i.e. the runtime, to run it.
切成：["Use Node.", "js, i.", "e.", "the runtime, to run it."]   ← 4 段垃圾
```

`Node.js` 那个点被当成了句号，`i.e.` 也被拆开。每段都单独发给 AI，
AI 手里只有半截词，能翻对才怪。而且段数变多 → 请求变多 → **花的钱也变多**。

修之后：这一句就是一整段，`Node.js` 原样保留、`i.e.` 不断开。

**代价（要如实说）**：分段方式变了，以前存的翻译缓存跟新切法对不上。
所以这一版把缓存的版本号从 `cache.v1` 换成了 `cache.v2` ——
你之前的缓存记录**会全部作废**（只是下次划过的话要重新翻一遍，不影响任何设置和 key）。

### v2.1.3：补测试（代码没动，只改了一行注释）

v2.1.3 没有功能变化。它做的是**给 v2.1.2 修的东西补上自动化测试**，
以后改代码时这几个 bug 要是被改回来，测试会立刻报出来。

| 测试 | 测什么 |
|---|---|
| `tests/_test-v212-regression.js` | 36 项断言，逐条守住 v2.1.2 修的 5 个 bug |
| `tests/_test-cache-flush.js` | 18 项断言，测"退出时代理把缓存真写进磁盘" |
| `tests/_test-e2e.js` | 端到端：在假浏览器里模拟划词，让脚本真的翻一遍，验证渲染出来的内容 |
| `tests/_test-load.js` | 把整脚本在假浏览器里加载一遍，逐个调用 8 个油猴菜单命令 |
| `tests/_test-all.js` | 一把梭跑全部 11 个测试文件，最后给总账 |

跑法（要先启动代理）：

```powershell
cd 这个项目所在的文件夹
node tests/_test-all.js
```

代理没在跑的话，需要代理的那几个用例会自动跳过，不会报错。

**为什么值得专门写这一套**：这几个 bug 全都是"不报错、结果默默就是错的"型，
语法检查（`node --check`）一个都抓不到。`_test-e2e.js` 尤其有用 ——
它证明的不只是"函数算对了"，而是"划词之后气泡里真的显示出了 Node.js"。

### v2.1.4：开源前安全收敛（脚本本体一行没改）

**这一版脚本本身没有变化。** 改的是本机代理和文档，
递增版本号是为了给"正式发布的那一对"做个标记。

| 改了什么 | 为什么 |
|---|---|
| `/health` 不再返回密钥文件的完整路径 | 原来任何能访问本机 8787 端口的进程，都能从这个接口问出"你的密钥是从哪个文件读的"，**连用户名和目录结构一起送出去**。现在只返回"环境变量"或"配置文件"这种不含路径的标签。**密钥本身从来没有泄露过**——接口只返回"有没有读到"，不返回密钥内容 |
| README 增加「权限与安全」章节 | 主动说明为什么脚本需要 `@match *://*/*` + `@connect *` 这么宽的权限，并列出全部对外请求的出口（5 处，带行号），别人可以自己核对 |
| 如实写明一处已知边界 | 本机代理**没有鉴权**：你电脑上跑的任何程序都能调用它、消耗你的额度。这条**没有修**，理由是油猴脚本是明文、保管不了令牌；而加一个要手动粘贴的令牌只挡得住"本机已经中毒"这种情况，却让安装多一步。所以选择写进 README 而不是假装解决 |

**顺带确认一件事**：代理只监听 `127.0.0.1`，
**局域网里的其他电脑访问不到它**。这一条本来就是对的设计。

---



从「在 GitHub 上看英文太累」开始做的一个小工具，后来扩到全站可用。
代码全部由 AI 辅助编写，用 git 管理版本（见 `Git使用说明.md`）。
