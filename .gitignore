# ============================================================
#  .gitignore —— 绝不提交的文件
# ============================================================
#
# 这个文件的作用：告诉 git「这些文件别管」。
# 它最重要的使命是防止把下面这些东西传到 GitHub 上。
#
# ⚠️ 删掉这个文件、或删掉里面任何一行之前，先想清楚后果。
# ============================================================


# 🔴🔴🔴 最重要的一条：翻译缓存 🔴🔴🔴
#
# 这个文件里存着「你划过的原文 + 译文」，是明文。
# 里面有 100 多条你的真实划词记录 —— 相当于你的阅读和复制历史。
# 一旦提交上 GitHub，就等于公开了。
.translate-cache.json


# 🔴 密钥类文件（将来公开版可能会用到）
#
# 公开版会让用户填自己的 API key，那些 key 会存在浏览器扩展里，
# 但如果你哪天加一个本机配置文件，一定不能提交。
config.json
config.local.json
*.credentials.yaml
.env
.env.local


# 日志与临时文件
*.log
*.tmp
*.temp
.translate-proxy.log
.archive-path.tmp


# 依赖目录（本工具是零依赖，纯粹防患于未然）
node_modules/


# 冻结版本的分发副本（供 /v/xxx.user.js 单独安装测试用）
# 每个版本真正的历史记录在 git 里，这里只是给本机代理读的副本，不必进仓库
versions/


# 词典数据（ECDICT 的 csv / sqlite，体积大，不进仓库）
data/


# 编辑器与系统杂项
.vscode/
.idea/
*.swp
Thumbs.db
Desktop.ini
.DS_Store


# 本地发布清单：写给你自己看的，里面有你的本地路径说明，不要进仓库
发布前必读.md
